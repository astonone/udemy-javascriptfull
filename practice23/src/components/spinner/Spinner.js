import './spinner.scss';
import spinner from "../../resources/img/Fidget-spinner.gif";

const Spinner = () => {
    return (
        <img src={spinner} alt="spinner" className="spinner"/>
    )
}

export default Spinner;